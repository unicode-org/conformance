name: dart_executor
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  collection: ^1.19.1
  intl4x: 0.17.0
  node_preamble: ^2.0.2
  pubspec_lock_parse: ^2.2.0

dev_dependencies:
  lints: ^6.0.0
  meta: ^1.17.0
  test: ^1.28.0


hook:
  intl4x:
    buildMode: fetch
