name: dart_executor
publish_to: none

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  intl4x: ^0.9.1
  pubspec_lock_parse: ^2.2.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.2
