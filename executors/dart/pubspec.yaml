name: dart_executor
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  collection: ^1.19.1
  intl4x: 1.0.0-alpha.1
  node_preamble: ^2.0.2
  pubspec_lock_parse: ^2.2.0

dev_dependencies:
  lints: ^6.1.0
  meta: ^1.18.3
  test: ^1.31.2


hook:
  intl4x:
    buildMode: fetch
