# TODO: Deduplicate this file when only the unified CLI is used.
from os.path import join

paths = {
    "logging_config": join("..", "logging.conf"),
    "default_input": join("..", "DDT_DATA", "testData"),
    "default_output": join("..", "DDT_DATA", "testOutput"),
    "default_report": join("..", "DDT_DATA", "testReports"),
}
